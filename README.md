## 数据初始化

首次启动应用时，会自动使用 `data.py` 中内置的示例数据创建 SQLite 数据库（`projects.db`）。如需重置，请删除 `projects.db` 并重启应用。

<img width="1425" height="815" alt="image" src="https://github.com/user-attachments/assets/36226ce9-ab81-4037-878f-e0d36c854cda" />
