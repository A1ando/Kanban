version: '3.8'

services:
  gantt-app:
    build: .
    container_name: gantt-app
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/projects.db
    restart: unless-stopped
